# مراجعة المتجر — 5 سبتمبر 2026

## الإصلاحات المنجزة

- اعتماد ملف logo.jpg المرسل من المستخدم حرفيًا؛ SHA-256 مطابق للنسخة المصدر. الشعار الجديد في الهيدر والفوتر والإدارة وشاشة التحميل وأيقونة الموقع، بلا وهج CSS.
- شاشة تحميل بتصميم بطاقة ملابس معلّقة وخطوط خياطة وألوان الشعار، مع تخطيط للهاتف والحاسوب والعربية والإنجليزية وتقليل الحركة.
- تحميل صور الكتالوج والمعارض والمصغرات والصور المتجاوبة الظاهرة، وانتظار فك ترميز الصور الناجحة. ستة طلبات متوازية مع إعادة محاولة ومهلة لكل صورة، وتنظيف callbacks عند تغير البيانات.
- لا تُحسب الصور الفاشلة ضمن الجاهزة. تظهر خيارات إعادة المحاولة أو تصفح المتاح عند الخطأ، بما يشمل انقطاع خدمة الكتالوج.
- إلغاء علامة localStorage الدائمة غير الصحيحة. الزيارة التي تجهّز صورها خلال 400ms لا تظهر فيها الشاشة؛ البيانات الجديدة أو الصور التي خرجت من ذاكرة المتصفح يعاد التحقق منها.
- صور المنتجات الفاشلة تستخدم رسم قطعة ملابس محايدًا بدل شعار المتجر، ويمكنها استعادة الصورة الأصلية بعد نجاح إعادة التحميل.
- السبب الفعلي للبطء: 104 صور data URL داخل 26 منتجًا. استخراجها إلى ملفات ثابتة مطابقة بالبايت، مع مطابقة SHA-256 تمنع استبدال صورة معدلة بنسخة قديمة. بقيت بيانات المصدر في Firebase دون تعديل.
- الحجم المقاس لبيانات الكتالوج بعد تحويل الروابط: 36,347 بايت بدل نحو 18.33MiB. هذا قياس JSON، وليس وعدًا بتحميل جميع الصور فورًا؛ أول زيارة ما زالت تحتاج تنزيل ملفات الصور عبر اتصال الزبون.
- ملفات الصور ذات الأسماء المشتقة من المحتوى قابلة للكاش لمدة سنة. الصور الجديدة من الإدارة تبقى على مسار الرفع المعتاد.
- إعداد D1 وإجراء migration وربط DB للإنتاج، وإعداد Turnstile ومفتاح Firebase العام وملح حماية الطلبات. الأسرار محفوظة مشفرة لدى Cloudflare، وليست في Git.

## التحقق

- pnpm check: 20 اختبارًا ناجحًا وبناء إنتاج ناجح.
- python test/loading_test.py: تحميل بطيء، عدم احتساب الفشل كنجاح، إعادة محاولة، زيارة سريعة دون وميض، تجاهل العلم القديم، انقطاع الاتصال، reduced motion، هاتف وحاسوب.
- python test/storefront_test.py: رابط منتج مباشر، صور بديلة، تصفية، حفظ السلة والمفضلة، هاتف عربي، فشل الطلب دون فقد السلة، استجابة نجاح محاكية وفاتورة، دون أخطاء JavaScript.
- مراجعة لقطات شاشة التحميل للهاتف والحاسوب بصريًا.
- فحص النسخة المنشورة عبر Chromium: الصفحة 200؛ /api/health أعاد database=ready وsecurity=ready؛ /api/catalog أعاد 26 منتجًا و104 روابط صور ثابتة وحجم JSON يساوي 36,357 بايت. اكتمل اللودنج دون استجابات صور فاشلة أو أخطاء JavaScript. رفض HTTP 1010 خاص بفحص urllib المباشر؛ المتصفح يصل بصورة طبيعية.
- اختبار تطابق ملفات الصور الـ104 مع مفاتيح التجزئة؛ عدم استبدال صورة جديدة أو رابط بعيد.
- اختبارات الطلبات محلية باستجابات محاكية؛ لم يتم إنشاء طلب تجاري وهمي أو تغيير مخزون الإنتاج.

## ملاحظات تشغيلية

- قاعدة D1 جديدة للطلبات التي يستقبلها هذا الإصدار؛ لم ننقل أي سجلات طلبات تاريخية خاصة من Firebase. تبقى سجلات Firebase القديمة محفوظة في مصدرها.
- Preview لا يرتبط بقاعدة طلبات الإنتاج؛ يتطلب إعداد قاعدة ومفاتيح اختبار مستقلة عند الحاجة.
- المصدر القديم داخل Firebase ما زال يحمل الصور المضمّنة؛ يحوّل الخادم الاستجابة العامة إلى روابط صغيرة. مسار fallback المباشر إلى Firebase ما زال يحتاج تنزيل البيانات القديمة إذا تعطّل API.
- لا يمكن ضمان خلو أي مشروع من كل الأخطاء؛ تغطي هذه المراجعة المسارات والاختبارات المذكورة، دون اختبار دخول مدير فعلي أو جهاز iPhone فعلي.
