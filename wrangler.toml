# Cloudflare Pages project configuration.
# Lets `wrangler pages deploy` (and the dashboard) know how the site is built.
name = "iraqstore"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"
